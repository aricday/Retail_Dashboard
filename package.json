{
  "name": "retailnext-pulse-fullstack-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix frontend",
    "start:api": "node --env-file=.env backend/server.js",
    "start:web": "npm run dev --prefix frontend",
    "build:web": "npm run build --prefix frontend"
  }
}